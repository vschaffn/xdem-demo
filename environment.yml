name: xdem-demo
channels:
  - conda-forge
dependencies:
  - python>=3.8
  - jupyter
  - jupytext
  - numpy
  - matplotlib
  - rasterio

  - pip:
      - git+https://github.com/GlacioHack/geoutils.git
      - git+https://github.com/GlacioHack/xdem.git
